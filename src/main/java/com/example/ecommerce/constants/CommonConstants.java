package com.example.ecommerce.constants;

/**
 * Utility class that holds common constants values used across the application.
 */
public final class CommonConstants {

    private CommonConstants() {} // prevent instantiation

    /**
     * A reusable empty string constant.
     */
    public static final String EMPTY_STRING = "";

    /**
     * The HTTP {@code Authorization} header name.
     */
    public static final String AUTH_HEADER = "Authorization";

    /**
     * The standard {@code "Bearer "} prefix for token-based authentication.
     */
    public static final String BEARER_PREFIX = "Bearer ";
}
